<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.expensesmanager.repository.ItemMapper">

  <select id="findAll" resultType="com.example.expensesmanager.entity.Item">
    SELECT id, name, cost, created_at FROM items ORDER BY created_at DESC;
  </select>

  <select id="selectById" resultType="com.example.expensesmanager.entity.Item">
    SELECT id, name, cost, created_at FROM items WHERE id = #{id};
  </select>

  <select id="insert" resultType="com.example.expensesmanager.entity.Item">
    INSERT INTO items (name, cost)
    VALUES (#{name}, #{cost})
    RETURNING id, name, cost, created_at
  </select>

  <update id="update">
    UPDATE items
    SET name = #{name}, cost = #{cost}
    WHERE id = #{id}
  </update>

</mapper>